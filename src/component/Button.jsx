import React from 'react'

const Button = (props) => {
    // let haha=props.text
  return (
   <div>
      <button>{props.text}</button>  
      <button>{props.haha}</button>
      </div>
  )
}

export default Button